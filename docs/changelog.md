# Changelog (High-Level)

Recent Waves:

- Manifest consolidation (OpenShift base simplification, internal registry via images block).
- Asynchronous model pre-download (LLM + Embeddings) & frontend notifications.
- Dependency & permission fixes (langchain-huggingface, HuggingFace cache ownership).

Detailed historical notes archived in `docs/archive/`.
