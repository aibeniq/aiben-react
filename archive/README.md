# Historical artifacts

This directory groups material retained for reference but not used by the
application or CI:

- `generated-data/` contains temporary translation data.
- `legacy-config/` contains superseded Docker Compose configuration.
- `manual-tests/` contains standalone diagnostics, test scripts, logs, and
  fixtures.
