apiVersion: v1
kind: Service
metadata:
  name: adminer-fixed
  namespace: aibeniq-prod
  labels:
    app: aibeniq
    component: adminer-fixed
    environment: production
    version: v1
spec:
  selector:
    app: aibeniq
    component: adminer-fixed
    environment: production
    version: v1
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  type: ClusterIP
